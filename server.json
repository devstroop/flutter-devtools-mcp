{
  "name": "flutter_devtools_mcp",
  "version": "0.3.0",
  "description": "MCP server for Flutter DevTools automation. ATTACH to a running app by passing its VM Service URL to connect, or use launch to start one.",
  "command": "bin/flutter_devtools_mcp_server",
  "args": [],
  "tools": [
    {
      "name": "launch",
      "description": "Launch a Flutter app in debug mode. Starts flutter run, waits for VM Service URL, auto-connects. Use stop_app to kill it.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "projectPath": {
            "type": "string",
            "description": "Absolute path to the Flutter project directory"
          },
          "deviceId": {
            "type": "string",
            "description": "Target device (macos, ios, android, chrome). Default: macos"
          },
          "target": {
            "type": "string",
            "description": "Target file. Default: lib/main.dart"
          },
          "launchTimeout": {
            "type": "number",
            "description": "Max seconds to wait. Default: 120"
          }
        },
        "required": ["projectPath"]
      }
    },
    {
      "name": "launch_status",
      "description": "Check the status of the most recently launched app process."
    },
    {
      "name": "stop_app",
      "description": "Stop a running app launched via the launch tool. Kills flutter run and disconnects."
    },
    {
      "name": "discover",
      "description": "Scan for running Flutter debug apps via mDNS. Returns ws:// URLs. Pass one to connect to attach."
    },
    {
      "name": "status",
      "description": "Check connection status. Shows URL if connected, and how many apps detectable via mDNS."
    },
    {
      "name": "connect",
      "description": "ATTACH to a running Flutter debug app via its VM Service URL. Pass the http:// URL from 'flutter run' output (with the auth token) as vmServiceUrl. Auto-normalizes http:// to ws://. No args = auto-discover via mDNS.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "vmServiceUrl": {
            "type": "string",
            "description": "The VM Service URL from flutter run output, e.g. http://127.0.0.1:54321/abc123=/. Both http:// and ws:// work."
          }
        }
      }
    },
      "name": "connect",
      "description": "Connect to a Flutter debug app. No args = auto-discover via mDNS. Pass vmServiceUrl to connect explicitly.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "vmServiceUrl": {
            "type": "string",
            "description": "VM Service WebSocket URL (optional — auto-discovers via mDNS if omitted)"
          }
        }
      }
    },
    {
      "name": "snapshot",
      "description": "Requires: connect first. Get the current widget tree as LLM-friendly JSON.",
      "inputSchema": { "type": "object", "properties": {} }
    },
    {
      "name": "inspect",
      "description": "Requires: connect first. Get detailed properties of a specific widget node.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "nodeId": { "type": "string", "description": "Node ID from snapshot" }
        },
        "required": ["nodeId"]
      }
    },
    {
      "name": "tap",
      "description": "Requires: connect first. Tap a widget by selector (semantics:, key:, text:, index:).",
      "inputSchema": {
        "type": "object",
        "properties": {
          "selector": { "type": "string", "description": "Widget selector" }
        },
        "required": ["selector"]
      }
    },
    {
      "name": "type_text",
      "description": "Requires: connect first. Focus a text field by selector and enter text.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "selector": { "type": "string", "description": "Text field selector" },
          "text": { "type": "string", "description": "Text to enter" }
        },
        "required": ["selector", "text"]
      }
    },
    {
      "name": "scroll",
      "description": "Requires: connect first. Scroll a scrollable widget in a direction.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "selector": { "type": "string", "description": "Scrollable widget selector" },
          "direction": { "type": "string", "enum": ["up", "down", "left", "right"], "description": "Scroll direction" },
          "amount": { "type": "number", "description": "Scroll amount in pixels (default: 300)" }
        },
        "required": ["selector", "direction"]
      }
    },
    {
      "name": "screenshot",
      "description": "Requires: connect first. Capture the current screen as PNG.",
      "inputSchema": { "type": "object", "properties": {} }
    },
    {
      "name": "hot_reload",
      "description": "Requires: connect first. Trigger hot reload on the connected Flutter app.",
      "inputSchema": { "type": "object", "properties": {} }
    },
    {
      "name": "evaluate",
      "description": "Requires: connect first. Evaluate a Dart expression in the running app.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "expression": { "type": "string", "description": "Dart expression" }
        },
        "required": ["expression"]
      }
    },
    {
      "name": "press_back",
      "description": "Requires: connect first. Press the system back button / pop the top route.",
      "inputSchema": { "type": "object", "properties": {} }
    },
    {
      "name": "toggle_dark_mode",
      "description": "Requires: connect first. Toggle dark/light mode."
    },
    {
      "name": "toggle_platform",
      "description": "Requires: connect first. Override the target platform (android, ios, ...)."
    },
    {
      "name": "get_memory",
      "description": "Requires: connect first. Get memory usage of the Flutter app."
    },
    {
      "name": "dump_semantics",
      "description": "Requires: connect first. Dump the accessibility/semantics tree."
    },
    {
      "name": "hot_restart",
      "description": "Requires: connect first. Full hot restart — resets all app state."
    },
    {
      "name": "get_errors",
      "description": "Requires: connect first. Get Flutter framework errors."
    },
    {
      "name": "toggle_debug_paint",
      "description": "Requires: connect first. Toggle widget boundary overlay."
    },
    {
      "name": "toggle_repaint_rainbow",
      "description": "Requires: connect first. Toggle repaint rainbow overlay."
    },
    {
      "name": "toggle_slow_animations",
      "description": "Requires: connect first. Slow down or restore animation speed."
    },
    {
      "name": "toggle_performance_overlay",
      "description": "Requires: connect first. Toggle frame timing graphs overlay."
    },
    {
      "name": "get_render_tree",
      "description": "Requires: connect first. Dump the render object tree as text."
    },
    {
      "name": "get_layer_tree",
      "description": "Requires: connect first. Dump the compositing layer tree."
    },
    {
      "name": "get_parent_chain",
      "description": "Requires: connect first. Get parent chain for a node ID."
    },
    {
      "name": "track_rebuilds",
      "description": "Requires: connect first. Toggle widget rebuild tracking."
    },
    {
      "name": "track_repaints",
      "description": "Requires: connect first. Toggle repaint tracking."
    },
    {
      "name": "get_logs",
      "description": "Requires: connect first. Capture recent app output logs."
    }
  ]
}
