name: flutter_devtools_mcp
description: MCP server for Flutter UI automation via DevTools VM Service extensions.
version: 1.0.3
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  vm_service: ^14.0.0
  web_socket_channel: ^3.0.0
  args: ^2.4.0
  logging: ^1.2.0
  meta: ^1.15.0

dev_dependencies:
  test: ^1.24.0
  lints: ^4.0.0
  mockito: ^5.4.0
  build_runner: ^2.4.0
